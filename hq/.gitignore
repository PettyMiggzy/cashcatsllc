node_modules/
dist/
build/
.env
.env.local
*.log

# Licensed art still pending purchase (tileset, item icons) — bought on your
# own account, added at deploy time, NEVER committed. This repo is public;
# see ASSETS.md. Our OWN map.json / map.tmx (authored in Tiled, phase 04) is
# NOT covered by this and should be committed normally.
client/public/assets/tileset/**/*.png
client/public/assets/items/**/*.png
client/public/assets/map/**/*.png
!client/public/assets/**/.gitkeep

# Character sprites ARE committed — free CC-BY 3.0, redistribution allowed
# with credit (see ASSETS.md / README). client/public/assets/character/*.png
# and client/src/images/**/*.png are intentionally NOT excluded here.
